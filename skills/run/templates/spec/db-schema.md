# DB Schema: <spec-name>

> 이 문서는 **물리 스키마 계층**이다 — 테이블·컬럼·인덱스·제약·마이그레이션.
> 도메인 개념(엔티티·식별 규칙·상태 전이)은 `data-model.md`가 다룬다. 이 문서는 그 도메인 모델을
> RDB로 내린 결과를 적는다. `plan.md`의 하위 문서다.
>
> 내용은 이번 spec이 추가·변경하는 스키마 **변경분(delta)**이다.
> 전체 스키마의 현재 진실은 루트 `docs/db-schema.md`이며, Stage 8(Root Sync)에서 그쪽을 현재 상태로 갱신한다(이번에 안 바뀐 부분은 보존).
> 작업 종료 후 이 문서는 stale해질 수 있으며, 그대로 둔다(과거 변경 기록).

---

## 개요

- 이 spec이 사용하는 테이블과 컬럼 변경 범위를 적는다.

## 신규 테이블

- 테이블명
- 목적
- 주요 컬럼

## 변경 테이블

- 변경 대상
- 변경 이유

## 인덱스

- 필요한 인덱스와 목적을 적는다.

## 데이터 무결성

- 제약조건과 정합성 규칙을 적는다.

## 마이그레이션 고려사항

- 배포 순서, 백필, 롤백 이슈를 적는다.
