# Step <N> — <kebab-case-slug>

> **헤더 문자열을 바꾸지 마라.** `## Acceptance Criteria`는 검증 스크립트가 정확히 이 문자열로 찾고,
> 코드블록은 ` ```bash ` 또는 ` ```sh `만 인정한다. 어긋나면 AC가 0개로 읽혀 검증이 실패로 처리된다.

## 목표

<이 step이 무엇을 완성하는가 — 한두 줄>

## 관련 문서

> 구현 전에 **읽을** 문서. 확인할 대상은 아래 `## 검증 대상`에 적는다.

- `<경로>` — <이 step에 필요한 부분>

## 검증 대상

> 이 step이 확인하는 시나리오. 괄호에 그 시나리오가 나온 요구사항·완료 기준을 적는다.
> 시나리오로 표현되지 않는 step(순수 리팩터·마이그레이션)은 요구를 직접 적거나 "기존 동작 유지"를 쓴다.

- SCN-00N <한 줄 제목> (FR-00N)

## 구현 지시

> 인터페이스와 핵심 제약 위주로 적고 내부 구현은 고정하지 않는다.

1. <무엇을 만드는가>
2. <…>

## 주의사항

- 하지 마라: <무엇>. 이유: <왜>

## Acceptance Criteria

```bash
<테스트 실행 명령>
# expect: 1
test -f <삭제됐어야 하는 파일의 저장소 루트 기준 상대경로>
```
