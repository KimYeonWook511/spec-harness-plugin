# Plan: <spec-name>

> 이 문서는 **이 spec의 총괄 설계서**다. "무엇을·왜"(spec.md)를 받아 "어떻게 만들 것인가"를 정한다.
> 입력: `spec.md` (미확정 마커가 해소된 상태). plan은 spec을 바꾸지 않는다 — 충돌이 있으면 Clarify로 되돌아간다.
> 요구와 `불변` 시나리오는 이 plan이 확정되어 Design이 닫힐 때 동결된다.

- 상태: Draft

---

## Summary

- 이 spec의 핵심 요구(한두 줄) + 그것을 푸는 기술적 접근(한두 줄). 상세는 아래 섹션·하위 문서에.

## 기술 맥락

> **이 작업에서 달라지는 것만** 적는다. 스택 고정값(언어·플랫폼·DB 종류 등)은 루트 컨벤션을
> 따르므로 반복하지 않는다. 해당 없으면 "해당 없음".

- **스택**: 이 저장소의 규칙 문서를 따른다. 이 작업에서 바뀌는 것만 아래 명시.
- **새 의존성**: <이 작업이 새로 도입하는 라이브러리·외부 서비스 / 없음>
- **성능·제약**: <응답시간·동시성·메모리 등 이 작업의 목표·제약 / 해당 없음>
- **규모·범위**: <데이터량·트래픽 가정 / 해당 없음>
- **미해결 기술 선택**: <조사가 필요한 unknown이 있으면 나열 → `research.md`에서 해소 / 없음>

## 원칙 점검

> **GATE**: 설계를 확정하기 전에 통과해야 한다. 이 저장소의 규칙 문서를 기준으로 점검한다.

- **위험영역 점검**: 이 spec이 결제·인증·데이터 모델·상태 전이에 닿는가? 닿는다면, spec.md에서
  그 부분이 **확정**되었는가? (미확정 마커가 남아 있으면 Clarify(3)로 되돌아간다 — 설계를 진행하지 않는다.)
- **참조 규약 위반 여부**: 이 설계가 이 저장소의 규칙 문서가 정한 구조·예외 처리 규약을 위반하는가?
  - 위반 없음 → 통과.
  - 위반이 불가피하면 → 아래 "복잡도·예외 기록"에 정당화를 적거나, 설계를 고친다. 원칙을 희석하지 않는다.

## 구조 결정

- 이 작업을 어떤 구조로 풀지의 핵심 결정. 어느 도메인·레이어에 무엇이 생기고 바뀌는지의 큰 그림.
- 상세 구조·데이터 흐름은 `architecture.md`로 내린다.

## 설계 문서 구성 (이 spec에서 작성할 하위 문서)

> 이 작업에 필요한 것만 체크해 작성한다. 불필요한 문서는 만들지 않는다.

- [ ] `architecture.md` — 구조·레이어·책임 배치·데이터 흐름 (구조 변경이 있으면)
- [ ] `data-model.md` — 도메인 엔티티·관계·식별·**상태 전이** (도메인 모델을 다루면)
- [ ] `db-schema.md` — 테이블·인덱스·마이그레이션 (물리 스키마 변경이 있으면)
- [ ] `api-spec.md` — API 계약 (API 추가·변경이 있으면)
- [ ] `adr.md` — 채택한 설계 결정 staging (기록할 결정이 있으면)
- [ ] `research.md` — 기술 선택 조사 (새 의존성·미해결 기술 선택이 있을 때만)

## Phase 구성 *(필수)*

> **몇 번에 나눠 통합·검증할 것인가.** phase는 "그 단위만으로 한 번 통합·검증할 가치가 있는 덩어리"다.
> 각 phase를 어떤 커밋들로 쪼갤지(step)는 Steps 단계의 일이므로 여기 적지 않는다.
>
> **기본값은 phase 1개**(`1-main`)다. 이름은 `<순번>-<slug>`이고 **순번은 개수와 무관하게 1부터** 시작한다.
> 아래 중 하나가 분명할 때만 여러 개로 나눈다.
> - 강한 선후 의존: 앞 phase가 끝나야 다음을 안전하게 시작할 수 있다.
> - 중간 검증 가치: 큰 작업에서 중간에 한 번 끊어 제대로 됐는지 확인하는 게 의미 있다.

<나누는 이유 한두 줄. 1개면 "통합 지점이 한 번이라 나누지 않는다".>

- **`<순번>-<slug>`**: <이 phase가 담는 것>. 이 phase 끝에 "<무엇이 검증되는가>".

## 복잡도·예외 기록

> 원칙 점검에서 불가피한 위반을 정당화해야 할 때만 채운다. 없으면 "해당 없음".

| 위반 항목 | 왜 필요한가 | 더 단순한 대안을 왜 못 쓰는가 |
| --- | --- | --- |
| | | |
