# Architecture: <spec-name>

> 이 문서는 `plan.md`가 정한 구조 결정을 **구조·레이어·데이터 흐름 수준으로 구체화**한 하위 문서다.
> 총괄 설계·기술 맥락·어떤 설계 문서를 만들지는 `plan.md`를 본다.
>
> 동시에 이 문서는 이번 spec 시점의 **변경 제안 스냅샷**이다.
> 시스템의 현재 진실은 루트 `docs/architecture.md`이며, Stage 8(Root Sync)에서 그쪽을 현재 상태로 갱신한다(이번에 안 바뀐 부분은 보존).
> 작업 종료 후 이 문서는 stale해질 수 있으며, 그대로 둔다(과거 설계 기록).

---

## 개요

- 이 spec이 현재 시스템 구조에 어떤 영향을 주는지 적는다.

## 변경 대상

- 영향을 받는 레이어와 모듈을 적는다.

## 설계 방향

- 핵심 설계 방향과 책임 분리를 적는다.

## 데이터 흐름

- 요청, 서비스, 저장, 외부 연동 흐름을 적는다.

## 예외 및 실패 처리

- 주요 실패 케이스와 처리 방식을 적는다.

## 테스트 포인트

- 반드시 검증해야 할 포인트를 적는다.
